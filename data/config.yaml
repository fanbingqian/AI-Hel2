model:
  default: deepseek-v4-flash
  max_tokens: 8192
agent:
  toolsets:
    - hermes-cli
    - search
    - memory
    - file
    - nexus
platform_toolsets:
  api_server:
    - hermes-cli
    - search
    - memory
    - file
    - nexus

platforms:
  api_server:
    enabled: true
    extra:
      port: 18642
      host: "127.0.0.1"
      key: "aihel2-local-dev"
      model_name: "deepseek-v4-flash"
